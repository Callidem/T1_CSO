#!/bin/sh

case "$1" in
    start)
        echo "Iniciando busybox httpd como usuario httpd..."
        /usr/sbin/httpd -h /var/www -u httpd
        ;;
    stop)
        echo "Parando busybox httpd..."
        killall httpd
        ;;
    restart)
        echo "Reiniciando busybox httpd..."
        killall httpd
        /usr/sbin/httpd -h /var/www -u httpd
        ;;
    *)
        echo "Uso: $0 {start|stop|restart}"
        exit 1
        ;;
esac

exit 0
